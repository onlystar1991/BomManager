<%= form_for(part_module) do |f| %>
  <% if part_module.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(part_module.errors.count, "error") %> prohibited this part_module from being saved:</h2>

      <ul>
      <% part_module.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
