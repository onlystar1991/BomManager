<%= form_for(bom_category) do |f| %>
  <% if bom_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bom_category.errors.count, "error") %> prohibited this bom_category from being saved:</h2>

      <ul>
      <% bom_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
